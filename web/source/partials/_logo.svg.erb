<%# The wordmark. Its paths are in partials/_logo_symbol.svg.erb, which the layout renders one
    time at the end of the body, thus a page holds the geometry one time and not two. %>
<svg aria-hidden="true" focusable="false" viewBox="0 0 741.59 202.81"><use href="#site-logo"></use></svg>
