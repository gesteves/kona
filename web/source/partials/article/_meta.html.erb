<%# The meta line with the publish date. The full entry, the compact entry, and the Short share it,
    and they show the draft label and the tags. The summary card also shares it, with `show_draft`
    and `show_tags` set to false. The code parses the date one time, here. %>
<% render_draft = !defined?(show_draft) || show_draft %>
<% render_tags = !defined?(show_tags) || show_tags %>
<% published = published_datetime(article) %>
<%# `tracking_section` is an optional local. It tags the permalink for the analytics. ⚠️ The full
    entry must not pass it: on the page of the article that link is the page itself. %>
<% tracking_section = defined?(tracking_section) ? tracking_section : nil %>
<p class="entry__meta"
  data-controller="publish-date"
  data-publish-date-datetime-value="<%= published.iso8601 %>"
  data-publish-date-time-zone-value="<%= site_time_zone %>"
  data-publish-date-entry-type-value="<%= article.entry_type %>"
  data-publish-date-draft-value="<%= article.draft %>">
  <% if render_draft && article.draft %>
    <%= draft_badge %>
  <% end %>
  <span class="entry__highlight" data-publish-date-target="badge" hidden><%= icon_svg("classic", "regular", "sparkles") %> New</span>
  <span>
    <span data-publish-date-target="clock" hidden><%= clock_icon_svg(published) %></span>
    <span data-publish-date-target="calendar"><%= icon_svg("classic", "light", "calendar") %></span>
    <%= article_permalink_timestamp(article, link_class: article_click_classes(tracking_section)) %>
  </span>
  <% if render_tags && article.contentful_metadata.tags.present? %>
    <span>
      <%= tag_list_icon(article.contentful_metadata.tags.size) %>
      <span class="entry__tags" role="list" aria-label="Tags">
        <%= tag_chain_links(tag_breadcrumb_concepts(article).map { |tag| [tag.short_name, tag.path] }) %>
      </span>
    </span>
  <% end %>
</p>
