---
layout: error
title: Request blocked.
summary: Your request was blocked by this site’s security rules.
body: |
  Your request was blocked by this site’s security rules.
details: true
---

<%# Cloudflare Error Page type: WAF block (`waf_block`), served with a 403.
    No page-type token is required, but rule 1 in the zone's custom rules is knowingly
    over-broad (see the root CLAUDE.md), so a real reader landing on this page is an expected
    outcome — hence the ray ID. Deliberately NO link to the contact page: whatever blocked
    this request would block that one too, so pointing there is a dead end. %>
<%= partial 'partials/error' %>
