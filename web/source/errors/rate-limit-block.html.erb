---
layout: error
title: Slow down a moment.
summary: You’ve made too many requests in a short time. Please wait a minute and try again.
body: |
  You’ve made too many requests in a short time. Please wait a minute and try
  again.
details: true
---

<%# Cloudflare Error Page type: Rate limiting block (`ratelimit_block`), served with a 429.
    No page-type token is required. This is what the zone's two rate limiting rules serve: the
    /api/contact limiter and the site-host crawler brake (see the root CLAUDE.md). %>
<%= partial 'partials/error' %>
