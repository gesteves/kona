Sitemap: <%= full_url('/sitemap.xml') %>

User-agent: *
Allow: /
Allow: /cdn-cgi/image/
Disallow: /cdn-cgi/
<%# An unpublished draft renders at /id/<entry id>/, thus a person can read it and no list contains
    it. Each draft also has <meta name="robots" content="noindex">. This rule covers a crawler that
    never gets the page. %>
Disallow: /id/
