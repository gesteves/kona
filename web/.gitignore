.bundle
.cache
.DS_Store
.sass-cache
build/
.env
# Everything under data/ is generated by `rake import`, except the two checked-in inputs. Scoped
# to the whole directory rather than `data/*.json`: that only reached one level and only .json,
# so a generated PNG (or anything in a subdirectory) would get committed.
data/
!data/font_awesome.yml
!data/srcsets.yml
node_modules

# Local Wrangler state (created by `wrangler dev`)
.wrangler
tmp/
prompts/

.agents/skills/
