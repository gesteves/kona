{
  "scripts": {
    "build": "esbuild source/javascripts/stimulus/index.js --bundle --minify --target=es2020 --outfile=tmp/dist/javascripts/site.js",
    "watch": "esbuild source/javascripts/stimulus/index.js --bundle --target=es2020 --outfile=tmp/dist/javascripts/site.js --watch=forever",
    "lint:scss": "stylelint \"source/stylesheets/**/*.scss\"",
    "lint:scss:fix": "stylelint \"source/stylesheets/**/*.scss\" --fix",
    "format": "prettier --write \"**/*.{js,json,md}\"",
    "format:check": "prettier --check \"**/*.{js,json,md}\"",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "pagefind": "pagefind --site build",
    "build:status": "node --env-file=.env scripts/netlify-builds.js status",
    "build:stop": "node --env-file=.env scripts/netlify-builds.js stop",
    "build:activate": "node --env-file=.env scripts/netlify-builds.js activate",
    "build:deploy": "node --env-file=.env scripts/netlify-builds.js deploy"
  },
  "dependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "@hotwired/turbo": "^8.0.23",
    "@netlify/functions": "^5.3.0",
    "@web.awesome.me/webawesome-pro": "^3.7.0",
    "esbuild": "^0.28.1",
    "pagefind": "^1.5.2",
    "wrangler": "^4.20.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.71",
    "@cloudflare/workers-types": "^5.20260708.1",
    "prettier": "^3.9.5",
    "stylelint": "^17.14.0",
    "stylelint-config-standard-scss": "^17.0.0",
    "stylelint-scss": "^7.2.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.7"
  }
}
