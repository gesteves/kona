<section class="stats">
  <div class="stats__stat">
    <span class="stats__meta"><%= "Activity".pluralize(data.strava.swim.activities + data.strava.bike.activities + data.strava.run.activities) %></span>
    <span class="stats__number"><%= data.strava.swim.activities + data.strava.bike.activities + data.strava.run.activities %></span>
    <span class="stats__meta">In the past month <%= "😬" if data.strava.swim.activities + data.strava.bike.activities + data.strava.run.activities == 0 %></span>
  </div>
  <div class="stats__stat">
    <span class="stats__meta">Swimming</span>
    <span class="stats__number"><%= formatted_number(data.strava.swim.distance, precision: 1) %></span>
    <span class="stats__meta"><%= "Kilometer".pluralize(data.strava.swim.distance) %> <%= "😬" if data.strava.swim.distance == 0 %></span>
  </div>
  <div class="stats__stat">
    <span class="stats__meta">Cycling</span>
    <span class="stats__number"><%= formatted_number(data.strava.bike.distance) %></span>
    <span class="stats__meta"><%= "Kilometer".pluralize(data.strava.bike.distance) %> <%= "😬" if data.strava.bike.distance == 0 %></span>
  </div>
  <div class="stats__stat">
    <span class="stats__meta">Running</span>
    <span class="stats__number"><%= formatted_number(data.strava.run.distance) %></span>
    <span class="stats__meta"><%= "Kilometer".pluralize(data.strava.run.distance) %> <%= "😬" if data.strava.run.distance == 0 %></span>
  </div>
</section>
