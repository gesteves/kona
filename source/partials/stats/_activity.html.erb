<div class="stats__stat">
  <span class="stats__meta"><%= activity %></span>
  <span class="stats__number" data-controller="units" data-units-imperial-value="<%= meters_to_miles(distance) %>" data-units-metric-value="<%= format_distance_number(distance) %>" title="<%= "#{format_distance(distance)} | #{meters_to_miles(distance)} miles" %>"><%= format_distance_number(distance) %></span>
  <span class="stats__meta"><span data-controller="units" data-units-imperial-value="miles" data-units-metric-value="<%= format_distance_unit(distance) %>"><%= format_distance_unit(distance) %></span> <%= "😬" if distance == 0 %></span>
</div>
