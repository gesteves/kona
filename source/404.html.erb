---
title: Page Not Found
summary: The page you were looking for could not be found.
---

<article class="entry entry--page" id="main-content" aria-labelledby="hed">
  <header class="entry__header">
    <h2 id="hed">
      <%= remove_widows(current_page.data.title) %>
    </h2>
  </header>
  <main class="entry__body">
    <p>The page you’re looking for can’t be found, but here are some recent articles you may enjoy instead:</p>
  </main>
</article>
<% articles = recent_articles %>
<% if articles.present? %>
  <section class="recirculation no-border">
    <ol class="content__list">
      <% articles.each do |item| %>
        <li class="content__list-item">
          <%= partial 'partials/article_summary', locals: { article: item } %>
        </li>
      <% end %>
    </ol>
  </section>
<% end %>
